import { PagePlaceholder } from '../../components/ui/PagePlaceholder';

export function RequestWizardPage() {
  return (
    <PagePlaceholder
      title="Создание заявки"
      text="На следующем шаге добавим мастер: категория проблемы, уточнение, срочность, контакты, описание и итоговый текст обращения."
    />
  );
}